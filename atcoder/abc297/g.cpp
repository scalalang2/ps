#include<bits/stdc++.h>

using namespace std;

// N개의 돌뭉치들이 있다.
// i-th뭉치에는 A_i개의 돌이 있다.
// Taro가 먼저 하고 Jiro가 후픽이다.
// 돌뭉치 하나를 선택하고 L ~ R 사이의 돌을 없앤다.
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);



    return 0;
}